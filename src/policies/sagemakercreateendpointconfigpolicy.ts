// ~~ Generated by projen. To modify, edit .projenrc.ts and run "npx projen".
import * as ccapi from '@pulumi/aws-native';
import * as pulumi from '@pulumi/pulumi';

export interface SageMakerCreateEndpointConfigPolicyArgs {
  /**
   * The name of the role to attach the policy to.
   */
  roleName: pulumi.Input<string>;

  /**
   * Name of the SageMaker endpoint configuration
   */
  endpointConfigName: pulumi.Input<string>;
}

/**
 * Gives permission to create an endpoint configuration in SageMaker
 */
export class SageMakerCreateEndpointConfigPolicy extends pulumi.ComponentResource {
  constructor(name: string, args: SageMakerCreateEndpointConfigPolicyArgs, opts?: pulumi.ComponentResourceOptions) {
    super('aws-policies:index:SageMakerCreateEndpointConfigPolicy', name, args, opts);
    const opt = {
      parent: this,
      ...opts,
    };
    new ccapi.iam.RolePolicy(`${name}-policy`, {
      roleName: args.roleName,
      policyDocument: {
        Version: '2012-10-17',
        Statement: [
          {
            Effect: 'Allow',
            Action: [
              'sagemaker:CreateEndpointConfig',
            ],
            Resource: [
              pulumi.interpolate`arn:${ccapi.getPartitionOutput(opt).partition}:sagemaker:${ccapi.getRegionOutput(opt).region}:${ccapi.getAccountIdOutput(opt).accountId}:endpoint-config/${args.endpointConfigName}`,
            ],
          },
        ],
      }
    }, opt);
    this.registerOutputs({});
  }
}