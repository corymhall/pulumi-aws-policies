// ~~ Generated by projen. To modify, edit .projenrc.ts and run "npx projen".
import * as ccapi from '@pulumi/aws-native';
import * as pulumi from '@pulumi/pulumi';

export interface KMSEncryptPolicyArgs {
  /**
   * The name of the role to attach the policy to.
   */
  roleName: pulumi.Input<string>;

  /**
   * ID of the KMS Key
   */
  keyId: pulumi.Input<string>;
}

/**
 * Gives permission to encrypt with KMS Key
 */
export class KMSEncryptPolicy extends pulumi.ComponentResource {
  constructor(name: string, args: KMSEncryptPolicyArgs, opts?: pulumi.ComponentResourceOptions) {
    super('aws-policies:index:KMSEncryptPolicy', name, args, opts);
    const opt = {
      parent: this,
      ...opts,
    };
    new ccapi.iam.RolePolicy(`${name}-policy`, {
      roleName: args.roleName,
      policyDocument: {
        Version: '2012-10-17',
        Statement: [
          {
            Effect: 'Allow',
            Action: [
              'kms:Encrypt',
            ],
            Resource: [
              pulumi.interpolate`arn:${ccapi.getPartitionOutput(opt).partition}:kms:${ccapi.getRegionOutput(opt).region}:${ccapi.getAccountIdOutput(opt).accountId}:key/${args.keyId}`,
            ],
          },
        ],
      }
    }, opt);
    this.registerOutputs({});
  }
}